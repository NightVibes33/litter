# Shared Third-Party

Shared third-party source dependencies live here.

- `codex`: OpenAI Codex submodule used by the shared mobile runtime.
- `ghostty`: pinned Ghostty renderer submodule used by Litter's mobile terminal work.

Submodules are intentionally pinned. Use the repository Make targets and patch scripts instead of manually moving these refs.
