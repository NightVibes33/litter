name: Retired PerplexityAI Asset Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Workflow retired
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "The old standalone PerplexityAI asset-pack workflow has been retired."
          echo "Alley Cat no longer uses a private Perplexity asset release path from this workflow."
