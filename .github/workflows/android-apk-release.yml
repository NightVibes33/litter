name: Android APK Release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Android builds disabled
        run: echo "Android builds are disabled for this fork; Litter currently ships iOS only."
