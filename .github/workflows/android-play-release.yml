name: Android Play Release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Android builds disabled
        run: echo "Android Play release builds are disabled for this fork; Litter currently ships iOS only."
